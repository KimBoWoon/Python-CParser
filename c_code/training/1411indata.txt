#include <stdio.h>

int T, H, W, N;

int main()
{
	FILE* fp;
	int i, j;
	fp= fopen( "input.txt", "r");
	fscanf(fp, "%d", &T);
	for (i = 0; i<T; i++)
	{
		fscanf(fp, "%d%d%d", &H, &W, &N);  
		printf("%d%02d\n", (N - 1) % H + 1, (N - 1) / H + 1); 
	}
	return 0;

}