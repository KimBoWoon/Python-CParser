#include <stdio.h>
#include <string.h> 
#include <stdlib.h>

int Palindrome(char* parr, int first, int last);

void main(void)
 {
	 FILE *in_file;
	int no_cases; 
	char srr[1000]; 
	int length;
	int result; 
	int i;
	
	length=strlen(srr); 
	
	
	in_file = fopen("input.txt","r");
	if(in_file == NULL) exit(1);
	fscanf(in_file,"%d",&no_cases);



	for(i=0; i<no_cases; i++){
		fscanf(in_file,"%s",&srr);
		Palindrome(srr,0,strlen(srr)-1);
		printf("%d\n", Palindrome(srr,0,strlen(srr)-1));
		
	}
}

int Palindrome(char str[], int first, int last)
{
if (last <= first)
return 1;
else if (str[first] != str[last])
return 0;
else
return Palindrome(str, first+1, last-1);
}
