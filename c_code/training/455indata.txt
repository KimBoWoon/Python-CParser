
#include<iostream>
#include<fstream>
#include<cstring>

using namespace std;

int gcd(int a, int b) {
	if (b == 0)
		return a;
	else
		return gcd(b,a%b);
	
}


int main()
{
	int testCase, num,a,b = 0;
	fstream instream("input.txt", ios::in);
	instream >> testCase;
	for (int q = 0; q < testCase; q++)
	{
		instream >> a;
		instream >> b;
		cout << gcd(a,b) << endl;
	}
}

