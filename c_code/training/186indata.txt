#include <iostream> 
#include <fstream>
using namespace std;
int M(int n)
{
	if(n>100)
		return n-10;
	return M(M(n+11));
}
int main()
{
	int t,N;
	ifstream inStream;
	inStream.open("input.txt");
	inStream>>t;
	for(int i=0; i<t; i++)
	{
		inStream>>N;
		cout<<M(N)<<endl;
	}
}