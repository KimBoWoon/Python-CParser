
#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>
#include<stdlib.h>

int McC(int n)
{
	if (n > 100)
		return (n - 10);
	else
		return McC(McC(n + 11));

}
void main(){
	FILE *in_file;
	int no_cases, no_data1,no_data2;
	int i;
	

	in_file = fopen("input.txt", "r"); 
	if (in_file == NULL) exit(1);

	fscanf(in_file, "%d", &no_cases);

	for (i = 0; i < no_cases; i++)
	{
		fscanf(in_file, "%d", &no_data1);
	
		printf("%d\n", McC(no_data1));
		
	}

	fclose(in_file);
}

