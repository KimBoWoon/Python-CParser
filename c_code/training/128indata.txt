
#include<stdio.h>

int m(int a){
	if (a > 100) return a - 10;
	else m(m(a+11));
}

int main(){

	FILE *f;
	int t, n;
	f = fopen("input.txt", "r");
	fscanf(f, "%d", &t);
	while (t--)
	{
		fscanf(f, "%d", &n);
		printf("%d\n", m(n));
	}
	return 0;
}