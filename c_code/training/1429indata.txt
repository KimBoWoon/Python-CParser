
	#include<iostream>
	#include<fstream>

	using namespace std;

	int main(){
		ifstream inFile("input.txt");
		int cases;
		inFile >> cases;
		while (cases--){
			int h, w, n, t;
			inFile >> h >> w >> n;
			n%h ? t = 0 : t = h;
			cout << (n%h+t) * 100 + (n-1) / h+1 << endl;
		}
	}