#include<stdio.h>
#include<stdlib.h>


int fib(int n);

void main(){

	FILE *file;

	int numCases;
	int i;
	int n1,n2;  


	file = fopen("input.txt","r");
	if(file == NULL) exit(1);

	fscanf(file,"%d",&numCases);

	for(i=0; i<numCases; i++){

	fscanf(file, "%d %d", &n1, &n2);

	printf("%d\n",gcd(n1,n2));
	}

	fclose(file);
}


int gcd(int a, int b)
{
if (b == 0) 
return a;
else 
return gcd(b, a%b);
}