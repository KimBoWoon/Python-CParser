#include<stdio.h>
#include<stdlib.h>
#include<ctype.h>

int Mac(int n);


void main(void)

{
	FILE *inFile;
	int i, j, k;
	int numCases;
	int count;
	int num;

	inFile = fopen("input.txt", "r");

	if (inFile == NULL) exit(1);
	fscanf(inFile, "%d", &numCases);

	for (i = 0; i < numCases; i++)
	{
		fscanf(inFile, "%d", &num);
	
		printf("%d\n",Mac(num));
		

	
	}

	fclose(inFile);

}

int Mac(int n)
{

	if (n > 100)
	{
		return n-10;
	}

	else if (n <= 100)
	{
		return Mac(Mac(n+11));
	}

}

