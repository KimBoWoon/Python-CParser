


#include <stdio.h>

#define COMPARE_BIG(A,B) (A>B)?A:B
#define COMPARE_SMALL(A,B) (A>B)?B:A

int Euclid(int num1, int num2){
	if(num2==0) return num1;	
	else if(num2>0) return Euclid(num2, num1%num2);	
}

int main(){
	int cases,data1,data2,i;

	FILE *infile;
	infile = fopen("input.txt","r");
	fscanf(infile,"%d",&cases);

	for(i=0;i<cases;i++){
		fscanf(infile, "%d %d", &data1,&data2);
		printf("%d\n", Euclid(COMPARE_BIG(data1,data2), COMPARE_SMALL(data1,data2)));
	}

	fclose(infile);
	return 0;
}