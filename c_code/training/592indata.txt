


#include <stdio.h>
#include <stdlib.h>

int fac(int n)															
{
	if( n<= 1 )
		return 1;
	else
		return n * fac(n-1);
}

void main(void)
{
	FILE *inFile;
	int i;
	int numCases;

	inFile = fopen("input.txt", "r");									
	if(inFile == NULL) exit(1);

	fscanf(inFile, "%d", &numCases);									

	for(i=0; i<numCases; i++)
	{
		int N = 0;														

		fscanf(inFile, "%d", &N);										

		printf("%d\n",fac(N));
	}

	fclose(inFile);														
}