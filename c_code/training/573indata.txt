#include <stdio.h>
#include <stdlib.h>

int cal(int a); 
void cal1(char a[], int b, int c); 
int num = 1;

int main(){
	FILE *infile;

	int nocases;
	int i;
	int n, m;
	char a[30];

	infile = fopen("input.txt", "r");

	if(infile == NULL)
	{
		printf("파일열기 오류\n");
		exit(1);
	}
	fscanf(infile, "%d", &nocases);

	for (i = 0; i < nocases; ++i)
	{
		
		fscanf(infile, "%d %d", &n,&m);
		a[0] = 'a'; a[1] = 'b'; a[2] = 'c'; a[3] = 'd'; a[4] = 'e'; a[5] = 'f'; a[6] = 'g'; a[7] = 'h'; a[8] = 'i'; a[9] = 'j'; a[10] = 'k'; a[11] = 'l';
		cal1(a, n, m-1);
	}

	return 0;
}

void cal1(char a[], int b, int c)
{
	int i;
	int num = 0;
	int q = cal(b - 1);

	if(b > 0){
		if (q <= c){
			num = c / q;
			c %= q;
		}
		printf("%c",a[num]);
		for (i = num; i <= 12; ++i)
			a[i] = a[i+1];

		cal1(a,b-1,c);

	}

	else if(b <= 0){
		printf("\n");
		return;
	}
}

int cal(int a){
	if(a > 1)
	{
		return a*cal(a-1);
	}

	return num;
}