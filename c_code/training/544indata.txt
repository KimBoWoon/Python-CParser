

#include <iostream>
#include <fstream>
#include <string>
using namespace std;
int a = 1;
int b = 0;

void swap(char* a, char* b)
{
	char temp;
	temp = *a;
	*a = *b;
	*b = temp;

}
int factorial(int n)
{

	  return (n <= 1 ? 1 : n * factorial(n - 1));

}
void permute(string str, int numOfStr, int sequence)
 {
	 int quotient; 
	 int remainderr; 
	 

	 quotient = (sequence-1)/factorial(numOfStr-1);
	 remainderr = (sequence-1)%factorial(numOfStr-1);

	 while(1){
		
	 int cnt;
	 if(quotient == 0 && remainderr == 0) break;
	 cnt = quotient;
	 if(quotient > 0) {
	 while(1) 
	 {
	   	swap(&str[cnt-1+b], &str[cnt+b]);
	 	cnt--;
	  	if(cnt == 0) break;

	 }
	}
	 if(remainderr == 1) {swap(str[numOfStr-2], str[numOfStr-1]); break;}
	 else if(remainderr == 0) {break;}
		
	 a++;
	 b++;
	 quotient = (remainderr)/factorial(numOfStr-a);
	 remainderr = (remainderr)%factorial(numOfStr-a);
	 
	 
	}

	for(int i = 0; i < numOfStr ; i++)cout << str[i];
		cout<< endl;
 	
}

int main()
{
	int numTestCases;
	int numOfStr;
	int sequence;
	ifstream inFile;
	inFile.open("input.txt");
	inFile >> numTestCases; 

	if(inFile.is_open() == false) {cout << "파일을 열 수 없습니다" <<endl;}

		for(int i = 0 ; i < numTestCases ; i++) {
			string str = "abcdefghijkl";
			inFile >> numOfStr >> sequence;
			a = 1;
			b = 0;
			permute(str, numOfStr, sequence);
			}
}

