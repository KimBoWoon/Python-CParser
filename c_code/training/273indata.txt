
#include<iostream>
#include<fstream>
#include<string>
using namespace std;

int gcd(int a, int b);
void addOfFrac(int numeOfA, int denoOfA, int numeOfB, int denoOfB);

int main()
{
	int count = 0;
	ifstream file;
	file.open("input.txt");
	file >> count;

	int numeOfA = 0, denoOfA = 0, numeOfB = 0, denoOfB = 0; 
	for (int i = 0; i < count; i++)
	{
		file >> numeOfA >> denoOfA >> numeOfB >> denoOfB;
		addOfFrac(numeOfA, denoOfA, numeOfB, denoOfB);
	}
	return 0;
}

int gcd(int a, int b)
{
	if (b == 0)
		return a;
	else
		return gcd(b, a%b);
}

void addOfFrac(int numeOfA, int denoOfA, int numeOfB, int denoOfB)
{
	int gcm = gcd(denoOfA, denoOfB); 
	int lcm = gcm*(denoOfA / gcm)*(denoOfB / gcm); 
	int deno = lcm, nume; 

	nume = (numeOfA*(lcm / denoOfA)) + (numeOfB*(lcm / denoOfB));
	gcm = gcd(nume, deno); 
	cout << nume / gcm << " " << deno / gcm<<endl;
}